# Minimal requirements for data preparation and model prototyping
# Use a virtual environment and pin exact versions with `pip freeze > requirements.txt`

pandas>=1.5,<2
pyarrow>=8.0.0
datasets>=2.7.0
transformers>=4.30.0
tokenizers>=0.13.3
scikit-learn>=1.2
tqdm>=4.64.0
regex>=2023.6.3
sentencepiece>=0.1.97
torch>=2.0.0  # optional: only if you plan to train/finetune with PyTorch
accelerate>=0.20.3  # optional: for distributed/accelerated training
pyyaml>=6.0

# Optional developer/test tools
black
flake8
